var assert = require('assert');
var PubSub = require('../lib/pubsub_service');
var DataStream = require('../lib/data_stream');

describe('DataStream', function() {
  it('exposes sub', function() {
  });
});
